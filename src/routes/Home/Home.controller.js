import { Badge } from 'bootstrap-vue/es/components';
import Button from 'COMPONENTS/Button/Button.template'

export default {
    name: 'home',
    props: {
    },
    components: {
        'Badge': Badge,
        'mybutton': Button
    }
}